/**
 * ============================================================
 *  MedoEDUz Config — единая точка настройки
 * ============================================================
 *
 *  Синхронизация прогресса между устройствами по Telegram user_id.
 *  Чтобы включить — вставьте сюда адрес развёрнутого Sync Worker
 *  (см. docs/telegram-setup.md, часть 2):
 *
 *      window.MEDOEDUZ_SYNC_URL = 'https://medoeduz-sync.ВАШ.workers.dev';
 *
 *  Пустая строка = синхронизация выключена, приложение работает
 *  чисто локально (безопасное значение по умолчанию).
 *
 *  Это ЕДИНСТВЕННОЕ место, которое нужно отредактировать после
 *  деплоя воркера. Копия этого файла лежит в app/ и синхронизируется
 *  сборщиком build-app.sh.
 */

window.MEDOEDUZ_SYNC_URL = '';
